package hr.project.repository
/*
import hr.project.model.Coach
import io.micronaut.data.annotation.Repository
import io.micronaut.data.repository.CrudRepository

@Repository
interface CoachRepository: CrudRepository<Coach, Long> {
    override fun findAll(): List<Coach>
    fun findById(id: Int): Coach
    fun save(coach: Coach): Coach
    fun update(coach: Coach): Coach
    fun deleteById(id: Int)
}
*/